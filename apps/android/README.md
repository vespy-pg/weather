# Android app

This directory is reserved for the native Android application and Jetpack Glance home-screen widgets. The Android client will consume the API described in `packages/weather-contract/openapi.yaml`.
