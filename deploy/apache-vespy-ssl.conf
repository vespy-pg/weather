<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName vespy.pl
    ServerAdmin webmaster@vespy.pl
    DocumentRoot /var/www

    RedirectMatch 301 ^/pogoda$ /pogoda/

    GeoIPEnable On
    GeoIPDBFile /usr/share/GeoIP/GeoIP.dat
    RequestHeader unset X-Visitor-Country
    RequestHeader set X-Visitor-Country "%{GEOIP_COUNTRY_CODE}e" env=GEOIP_COUNTRY_CODE

    ProxyPreserveHost On
    ProxyPass /pogoda/ http://127.0.0.1:18080/ connectiontimeout=5 timeout=30 retry=0
    ProxyPassReverse /pogoda/ http://127.0.0.1:18080/

    ErrorLog ${APACHE_LOG_DIR}/vespy-error.log
    CustomLog ${APACHE_LOG_DIR}/vespy-access.log combined

    SSLCertificateFile /etc/letsencrypt/live/vespy.pl/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/vespy.pl/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf
</VirtualHost>
</IfModule>
